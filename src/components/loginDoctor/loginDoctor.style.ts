import { makeStyles, createStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(thme =>
({
    box: {
        padding : 5,
        display: 'flex',
        flexDirection: 'row',
        margin: 0,
        backgroundColor: 'red',
    },
})
);

